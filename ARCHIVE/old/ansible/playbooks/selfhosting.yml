---
- name: Install self-hosting tools
  hosts: localhost
  tags: [selfhosting, self-hosting]
  gather_facts: yes

  roles:
    # Development tools for self-hosting
    - role: ansible-language-server
      tags: [lsp, dev, ansible]
