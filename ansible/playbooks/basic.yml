---
- name: Install all basics
  hosts: localhost

  handlers:
    - name: Source shell_config
      shell: . "{{ shell_config }}"
      args:
        executable: /bin/bash
      changed_when: false     

  roles:
    - lazygit
    - neovim
    - role: add_alias
      vars:
        alias_name: "vi"
        alias_cmd: "nvim"
